import Header from './header';
import Footer from './footer';
import Toast from './toast';
import Loader from './loader';

const Bases = {
  Header,
  Footer,
  Toast,
  Loader,
};

export default Bases;
export { Header, Footer, Toast, Loader };
